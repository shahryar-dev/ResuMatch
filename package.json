{
  "name": "resumatch",
  "private": true,
  "version": "0.2.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @resumatch/web",
    "build": "npm run build --workspace @resumatch/web",
    "lint": "npm run lint --workspace @resumatch/web",
    "preview": "npm run preview --workspace @resumatch/web"
  }
}
